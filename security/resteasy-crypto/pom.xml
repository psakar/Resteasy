<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.jboss.resteasy-ts</groupId>
        <artifactId>resteasy-jaxrs-all</artifactId>
        <version>2.3.7.Final</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <artifactId>resteasy-crypto</artifactId>
    <name>RESTEasy Crypto test</name>

    <properties>
        <version.org.jboss.resteasy.resteasy-crypto>${version.org.jboss.resteasy}</version.org.jboss.resteasy.resteasy-crypto>
    </properties>

    <dependencies>

        <dependency>
            <groupId>org.jboss.resteasy-ts</groupId>
            <artifactId>resteasy-test-tjws</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.jboss.resteasy</groupId>
            <artifactId>resteasy-crypto</artifactId>
            <version>${version.org.jboss.resteasy.resteasy-crypto}</version><!-- only upstream -->
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.jboss.resteasy</groupId>
            <artifactId>resteasy-jaxb-provider</artifactId>
            <version>${version.org.jboss.resteasy}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.jboss.resteasy</groupId>
            <artifactId>eagledns-fork</artifactId>
            <version>${version.org.jboss.resteasy}</version><!-- only upstream -->
            <scope>test</scope>
        </dependency>

    </dependencies>

	<profiles>
		<profile>
			<!-- exclude integrations tests which are known to fail-->
			<id>exclude-tests-known-failures-BC</id>
			<activation>
				<property>
					<name>exclude-tests-known-failures-BC</name>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-surefire-plugin</artifactId>
						<configuration>
							<excludes combine.children="append">
								<!-- requires jdk with unlimited cryptography without BouncyCastle JCE provider  -->
								<exclude>org/jboss/resteasy/test/security/smime/EnvelopedTest.java</exclude>
								<exclude>org/jboss/resteasy/test/security/smime/IntegrationTest.java</exclude>
								<exclude>org/jboss/resteasy/test/security/smime/SignedTest.java</exclude>
							</excludes>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
